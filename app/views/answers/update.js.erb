<% if @resource.errors.present? %>
  <% @resource.errors.full_messages.each do |message| %>
    $('.answer-errors').html('<%= j message %>')
  <% end %>
<% else %>
  $('#answer-<%= @resource.id %>').html('<%= j render @resource %>');
  $('#edit_form').remove();
<% end %>